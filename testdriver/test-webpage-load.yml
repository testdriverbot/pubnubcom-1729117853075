version: 4.0.69
steps:
  - prompt: Assert the webpage loads.
    commands:
      - command: assert
        expect: the webpage loads with "The complete real-time developer platform"
  - prompt: >-
      Assert the logo is visible on the page.  # Use the content of the file
      from the environment
    commands:
      - command: assert
        expect: the logo is visible on the page
